# Docusaurus

To run the documentation locally, follow the commands below:

### Install all packages

```shell
npm i
```

### Build the files

```shell
npm run build
```

### Start the server

```shell
npm start
```